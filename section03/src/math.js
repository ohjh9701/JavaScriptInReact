//math 모듈 (객체: function - 함수선언식, 함수표현식, 람다식)
//java = class

export default function add(a, b) {
  return a+b;
}

export function sub(a, b) {
  return a-b;
}

export function div(a,b) {
  return a/b;
}